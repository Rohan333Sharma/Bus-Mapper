<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BusMapper" parent="Theme.Material3.DayNight.NoActionBar">

        <item name="android:statusBarColor">@color/turquoise_blue</item>
        <item name="android:windowAnimationStyle">@anim/fade_in_activity</item>
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/turquoise_blue_500</item>
    </style>

    <style name="Theme.BusMapper" parent="Base.Theme.BusMapper" />
</resources>